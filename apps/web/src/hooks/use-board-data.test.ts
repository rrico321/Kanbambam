import { describe, it } from 'vitest'

// Stub: useBoardData hook loads data from API, not local state
// Will be expanded in Plan 03 when the hook is implemented
describe('useBoardData', () => {
	it.todo('fetches columns from API for the given boardId')
	it.todo('fetches items for each column')
	it.todo('returns sorted columns and itemsByColumn map')
	it.todo('returns isLoading true while fetching')
})

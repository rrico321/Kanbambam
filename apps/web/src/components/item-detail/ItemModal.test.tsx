import { describe, it } from 'vitest'

// Stub: ItemModal shows item fields and supports inline editing
// Will be expanded in Plan 05 when ItemModal is implemented
describe('ItemModal', () => {
	it.todo('renders item title, description, labels, and due date')
	it.todo('closes on Escape key')
	it.todo('closes on overlay click')
	it.todo('calls onClose when X button clicked')
})

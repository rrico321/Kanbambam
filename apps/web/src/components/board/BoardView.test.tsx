import { describe, it } from 'vitest'

// Stub: BoardView renders columns side by side
// Will be expanded in Plan 03 when BoardView is implemented
describe('BoardView', () => {
	it.todo('renders columns in horizontal scroll container')
	it.todo('shows loading skeleton while data loads')
	it.todo('shows empty state when board has no columns')
})

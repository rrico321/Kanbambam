import type { GlobalOptions } from '../../types.js'

export async function loginCommand(
	_options: { manual?: boolean },
	_globalOptions: GlobalOptions,
): Promise<void> {
	console.log('Login command not yet implemented. See Phase 2 Plan 03.')
}
